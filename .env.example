# Railway MCP Server URL (from your MCP service)
VITE_MCP_URL=https://your-mcp-service.railway.internal:8000

# NOTE: Do NOT put any API keys or tokens here.
# Anything prefixed with VITE_ is embedded into the public JS bundle.
# Auth must be handled server-side (see docs/admin-panel-plan.html).